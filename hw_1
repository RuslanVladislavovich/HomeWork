"""
1.1 Пользователь вводит число N, программа возвращает N-ный член последовательности Фибоначи.
"""
def fib(N):
    # Первые два члена = 1
    if N < 3:
        return 1

    first, second = 1, 1
    for i in range(3, N + 1):
        # Каждый следующий член - сумма двух предыдущих
        first, second = second, first + second
    return second

# Проверка
print(fib(7))
# Корректно

"""
1.2 Пользователь вводит число, программа проверяет, является ли оно простым.
Простое число - натуральное число, имеющее ровно два различных натуральных делителя (1 и само себя).
Т.е. если некоторое число num делится ещё хотя бы на какое-то число, кроме 1 и себя самого без остатка, 
то оно НЕ простое.
"""
def simple_num(num):
    # проверяем делимость num на все натуральные числа в интервале от 2 до (num-1) включительно
    for i in range(2, num, 1):
        if num % i == 0:
            # если такое нашлось, число - не простое
            return False
        # проверка делимости на 1 и на само себя
        if num % 1 == 0 & num % num == 0:
            return True
# Проверка
num = int(13)
if simple_num(num) is True:
    print(f'{num} - простое число')
else:
    print(f'{num} - НЕ простое число')

# НЕ корректно работает с некоторыми числами (3 - ошибка, возможно из-за выражения num-1)

"""
1.3 Вернуть простые делители введенного числа, или сообщить, что оно простое.
Вероятно, нужно составить ряд проверок с включением результата в список, после чего вернуть список.
"""
# зададим число
num = int(20)
# проверяем, является ли введённое число НЕ простым при помощи уже имеющейся функции simple_num(num)
if simple_num(num) is False:
    # заведём список для простых делителей
    list_of_simple_divisors = []
    # в диапазоне от 1 (хотя не обязательно) до заданного числа будем искать простые делители этого числа
    for i in range(1, num, 1):
        if simple_num(i) is True:
            list_of_simple_divisors.append(i)
    print(list_of_simple_divisors)
else:
    # сообщаем, что число простое
    print(f'{num} - простое число')

"""
1.4 Найти наибольший общий делитель (число, которое делит другое число без остатка) для двух введенных чисел
"""
# введём 2 произвольных числа
num1 = 8
num2 = 20
if num1 > num2:
    biggest = num1
else:
    biggest = num2
# заведём список общих делителей
list_divisors = []
for i in range(1, biggest, 1):
    if num1 % i == 0 & num2 % i == 0:
        list_divisors.append(i)
# выведем максимальное значение
print(max(list_divisors))
