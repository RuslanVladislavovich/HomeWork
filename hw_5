'''
5.1. Написать функцию, которая принимает как аргумент алфавит последовательности,
и возвращает функцию получения статистики встречаемости символов в последовательности.
'''


def fun(alphabet: str):
    'принимает строку-алфавит некоторой последовательности, возвращает функцию вычисления частот символов последовательности'

    def stat(seq: str):
        'вычисляет частотут встречаемости символов'
        # создадим словарь: ключи - это символы, значения - частота встречаемости в последовательности
        stat_lib = {}
        for i in alphabet:
            stat_lib[i] = 0
        for i in seq:
            if i in stat_lib:
                stat_lib[i] += 1
        print(stat_lib)
        return stat_lib
    return stat(seq)

alphabet = 'ATCG'
seq = 'AAATATATATTTTCGCGAAAT'
fun(alphabet)
